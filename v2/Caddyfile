{
	auto_https disable_redirects
}

https://localhost {
	tls internal
	root * .

	header {
		Cache-Control "no-store"
		Permissions-Policy "camera=(self), display-capture=(self)"
	}

	handle_path /v2/* {
		root * .
		file_server
	}

	handle {
		file_server
	}
}
